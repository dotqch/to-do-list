<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div class="title">
        <div class="px-5 py-4 my-4 text-center">
            <h1 class="display-5 fw-bold text-body-emphasis">To Do List (<%= list %>)</h1>
            <div class="col-lg-6 mx-auto">
                <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                <a href="/day"><button type="button" class="btn btn-primary btn-lg px-4 gap-3">Day's tasks</button></a>
                <a href="/work"><button type="button" class="btn btn-outline-secondary btn-lg px-4">Work tasks</button></a>
            </div>
            </div>
        </div>
    </div>
    <div class="addTask">
        <div class="px-5 my-4 col-md-8 col-lg-6 mx-auto">
            <div class="row p-3 bg-white rounded shadow-sm">
                <form action="/add" method="POST">
                    <div class="row align-items-center justify-content-center">
                        <div class="col">
                            <input class="form-control form-control-lg border-0 add-todo-input bg-transparent rounded" type="text" name="task" placeholder="Add new ..">
                        </div>
                        <div class="col-auto px-0 mx-2 mr-2">
                            <button type="submit" class="btn btn-primary">Add</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <div class="list">
        <div class="px-5 my-4 col-md-8 col-lg-6 mx-auto">
            <% for (let i = 0; i < tasksToDo.length; i++) { %>
                <div class="row py-3 px-5 border bg-white shadow-sm">
                <% if (tasksToDo[i].completed == false) {%>
                        <form class="p-0" action="/check" method="POST">
                            <div class="row align-items-center justify-content-center">
                                <div class="col p-0">
                                    <label for="index"><%= tasksToDo[i].task %></label>
                                    <input type="text" hidden=true name="index" value=<%= i %>>
                                </div>
                                <div class="col-auto px-0 mx-0">
                                    <input type="submit" value="Clear">
                                </div>
                            </div>
                        </form>
                <% } else { %>
                        <form class="p-0" action="/check" method="POST">
                            <div class="row align-items-center justify-content-center">
                                <div class="col p-0">
                                    <label for="index" class="text-muted"><s><%= tasksToDo[i].task %></s></label>
                                    <input type="text" hidden=true name="index" value=<%= i %>>
                                </div>
                                <div class="col-auto p-0">
                                    <input type="submit" value="Completed" disabled>
                                </div>
                            </div>
                        </form>
                <% } %>
                </div>
            <% } %>
        </div>
    </div>
    <div class="footer">

    </div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>